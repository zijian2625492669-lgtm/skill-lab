interface:
  display_name: "Excel 数据可视化"
  short_description: "读取 xlsx 字段，生成图表并运行基础统计分析。"
  default_prompt: "Use $excel-data-visualization to inspect an xlsx workbook, summarize fields, generate suitable charts, and run basic statistics."

policy:
  allow_implicit_invocation: true
