interface:
  display_name: "Excel 数据可视化"
  short_description: "读取 xlsx 字段并生成常用数据分析图表。"
  default_prompt: "Use $excel-data-visualization to inspect an xlsx workbook, summarize fields, and generate suitable charts."

policy:
  allow_implicit_invocation: true
